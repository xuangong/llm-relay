# LLM Relay v0.5.0

v0.5.0 改进了 Relay 的环境管理、配置恢复和桌面交互：Windows Host 与每个 WSL 发行版可以独立接管，官方 CLI 登录后保留新的登录状态，停用 Relay 会停止代理并释放端口。同步进度、运行状态和异常提示也更清晰。

## 环境独立管理与官方登录

- **Windows Host 与 WSL 分别控制**：设置中并列展示 Windows Host 和 WSL2 Distros，每个环境都有独立勾选项。取消一个环境会停止该环境内各 CLI 的接管并恢复配置，其他环境和全局客户端选择保持不变。
- **官方登录后自动交还配置**：检测到 Codex 或 Claude Code 的官方登录后，只取消对应环境的接管，并显示原因。保留该 CLI 的新凭证与账号状态，清理 Relay 路由；同环境的其他客户端按正常流程恢复原配置。
- **重新启用采用新的恢复基准**：登录后的清理成功才移除旧 `.origin`、`.bak` 等备份；清理失败会保留记录并重试。重新勾选环境后，下次应用以当时的配置作为新基准，避免旧备份覆盖新登录状态。
- **环境选择持续保留**：切换网关或再次点击 Use 不会自动勾回已取消的环境。所有环境都关闭时，总开关也会关闭；再次使用前需先选择环境。

登录识别基于可观察的配置与凭证文件变化，普通 token 刷新不作为新登录。系统凭据库、自定义 CLI home，以及部分旧版 Claude 的同账号重新登录可能无法识别。交还配置不会退出正在运行的 CLI，也不会修改它已继承的进程环境变量。

## 停用即停止服务

- **Disable Relay 移到主窗口**，同时显示代理实际运行状态与端口占用。
- 停用时先停止本机和 WSL 监听、断开正在转发的连接并释放端口，再恢复配置；恢复失败也保持停止。
- 停用状态跨重启保留，后台健康检查、自动故障转移和 WSL 重试不会重新启动代理。
- 已选环境下再次点击 Use 会启用 Relay，并先检查端口能否绑定；端口被其他程序占用时明确报错。

## 配置恢复与 WSL 同步

- **本地备份可自由编辑**：移除 `.llm-relay.origin` 和 `.llm-relay.bak` 的 SHA-256 校验，恢复时读取备份当前内容，旧清单中的哈希字段不再参与校验。
- **保留完整配置**：正常停用时保存当前工作配置到 `.bak` 并恢复 `.origin`；下次 Use 捕获当时的工作配置作为 `.origin`，再恢复已有 `.bak`。原文件不存在与空文件仍分别记录。
- **备份缺失不再卡住同步**：修复 checksum mismatch、lifecycle sidecar is missing 和 origin disappeared 等阻断场景。本地删除备份后，Use、Save 和 WSL 自动重试可以继续；恢复时缺少原有文件的备份则保留当前文件，实际读取错误仍会提示。
- **恢复记录缺失时可重建**：仍处于启用状态但恢复记录丢失时，优先从已有 `.bak` 重建，没有备份则从空配置生成；不会把 Relay 配置误存为原始配置。离线 WSL 重连后继续处理。
- **同步状态可见**：主窗口显示 WSL 异步同步进度和失败提示，设置中提供环境与文件详情；尚未完成时持续提示。
- **关闭选择不会被后台覆盖**：修复后台探测使用旧状态重新勾选已关闭 WSL 环境的竞态。

## 桌面界面与日常操作

- **设置侧边抽屉**：集中管理设备名称、语言、开机启动、自动故障转移和各环境选择，补齐 WSL 相关中文翻译。
- **主窗口状态更清晰**：保留使用指南、健康检查刷新和设置入口；自动故障转移关闭时显示提示。
- **关闭窗口后留在托盘或菜单栏**：Windows 关闭主窗口后不占任务栏，单击托盘图标打开、右击显示菜单；macOS 关闭窗口后隐藏 Dock 图标，保留菜单栏图标。托盘菜单、Dock 重新激活和第二实例启动都能正确唤起窗口。
- **网关操作**：支持行内重命名、置顶，以及在系统浏览器中打开网关地址。
- **设备命名**：修复未命名客户端无法点击名称编辑器的问题，统一在设置中修改。
- **TUI 初始化**：首次运行提供主密钥设置向导。

## CLI 兼容性、转发与日志

- **Claude Code 初始化**：补齐 `~/.claude.json` 的 onboarding 状态，让写入的配置可以正常使用；已完成初始化的状态保持不变，损坏文件原样保留并报错。
- **Codex Shell 配置**：统一使用 `llm-relay-ignore` 占位 key，按各环境的登录 Shell 选择启动文件和语法，原位更新已有赋值，保留用户真实 key。Windows Host 与各 WSL 环境分别处理。
- **所选 key 获取失败会明确报错**：当会话看不到所选 key 时尝试可用凭证，无法取得时停止应用，避免静默改用网关凭证而导致配额与请求归属错误。
- **上游响应与错误显示**：正确读取压缩响应，错误详情按字符截断，避免中文乱码；没有 model 的请求不再标记为 `[model:unknown]`。
- **按路径屏蔽错误日志**：支持屏蔽高频预检等路径；列表条数与 Errors 徽标均排除被屏蔽记录。
- **设备识别**：心跳增加操作系统机器标识，帮助网关识别同一台机器，减少重复设备记录；保留原有 `clientId`，无法读取机器标识时不发送占位值。

## 下载

| 平台 | 安装包 |
|------|--------|
| Windows x64 | [EXE 安装包](https://github.com/xuangong/llm-relay/releases/download/v0.5.0/LLM.Relay_0.5.0_x64-setup.exe) · [MSI 安装包](https://github.com/xuangong/llm-relay/releases/download/v0.5.0/LLM.Relay_0.5.0_x64_en-US.msi) |
| macOS Universal | [DMG 安装包](https://github.com/xuangong/llm-relay/releases/download/v0.5.0/LLM.Relay_0.5.0_universal.dmg) |

Windows 安装包包含上述最新改动，构建源码为 `a5748b9`。macOS DMG 保留此前的 v0.5.0 构建，尚未包含本次环境交还等后续改动。

macOS 安装包未做 Apple 公证。若首次打开出现“已损坏”或“无法验证开发者”，将应用拖入 `/Applications` 后执行：

```sh
xattr -dr com.apple.quarantine "/Applications/LLM Relay.app"
```

## 验证

131 项核心测试通过，覆盖环境隔离、官方登录后的凭证保留与恢复，以及全部环境关闭后的监听端口释放。桌面端和 agent 编译检查、前端类型检查与生产构建通过；未执行真实 OAuth 登录流程验证。
