# WSL2 Integration — Verification Log

Date: 2026-06-10
Branch: `worktree-wsl2-integration`
Host: Windows 11 Enterprise + Ubuntu-24.04 (WSL2 NAT mode, Docker Desktop installed and hijacking `host.docker.internal` to LAN IP)

## Automated

| # | Check | Status | Notes |
|---|-------|--------|-------|
| 1 | `cargo build --workspace` (debug) | PASS | clean compile, 1m36s |
| 2 | `cargo test --workspace` | PASS | all suites green (61+ tests across crates; only `ignored` are pre-existing platform-conditional tests, none WSL-related) |
| 3 | `pnpm typecheck` (`tsc --noEmit`) | PASS | no errors |
| 4 | `pnpm build:renderer` (vite production) | PASS | 2174 modules, 552kB JS |

## Manual scenarios

These require a Windows host with at least one WSL2 distro registered, plus a second
LAN host for the firewall check. Not runnable from this dev session — left as
NOT_RUN. Run before tagging a release.

| # | Scenario | Status | Notes |
|---|----------|--------|-------|
| 1 | Single distro (default Ubuntu) — `claude "hi"` in WSL succeeds | PASS | Full end-to-end on Ubuntu-24.04 with linuxbrew-installed claude. Probe falls through tier 1 (NAT) + tier 2 (DD-hijacked HDI), tier 3 injects `172.22.128.1 llm-relay-18081.host`. Apply writes distro `~/.claude/settings.json` `ANTHROPIC_BASE_URL=http://llm-relay-18081.host:18081`. `claude -p "respond with the single word: ping"` from inside the distro returns `ping` via dev relay → gateway → real model. |
| 2 | Multi-distro — install Debian, toggle in Settings, verify both `~/.claude/settings.json` written | NOT_RUN | needs ≥2 distros |
| 3 | Un-toggle Debian — verify Debian snapshot restored, Windows + Ubuntu untouched | NOT_RUN | follows from #2 |
| 4 | Disable Relay — Windows + all selected distros restored | PASS | Verified two sub-paths on Ubuntu-24.04: (a) with prior apply → wsl-<sha>.json snapshot restored, `/etc/hosts` cleaned via clear_targets_from_snapshots; (b) without prior apply (state-machine-injected hosts only, no snapshot) → defensive sweep in clear_active still removes the hosts entry. |
| 5 | `wsl --shutdown` then restart — Relay rebinds new gateway IP within 60s | NOT_RUN | requires WSL + state-machine tick observation |
| 6 | LAN check: `curl http://<my-LAN-IP>:18080` from another host → connection refused | NOT_RUN | needs second machine on LAN |
| 7 | `wsl --unregister Debian` while selected — apply/clear still works, warning logged | NOT_RUN | follows from #2 |
| 8 | Legacy migration: drop fake `~/.llm-relay/cli-config-backup.json`, restart Relay — migrates to `cli-config-backup/windows.json` | NOT_RUN | can be done on any Windows host |
| 9 | No-WSL Windows: Settings shows "No WSL2 distros detected", apply/clear normal | PARTIAL | UI shipped + verified visually via `pnpm build:renderer`; cannot fully verify list_wsl_distros runtime path without `wsl.exe` on PATH |
| 10 | mac/Linux regression: WSL panel not rendered, apply/clear unchanged | NOT_RUN | requires non-Windows host. The component short-circuits on `!isWindows`; the Rust commands are wired only in the Tauri builder and the Windows-only `wsl::network::find_wsl_gateway_ip` is `cfg`-gated. |

## Pre-tag checklist

Before promoting this branch to `main` / release:

1. Re-run automated checks #1–#4 above
2. Execute manual scenarios #1, #2, #3, #4, #5, #6 on a real Windows + WSL host
3. Execute scenario #10 on a macOS or Linux host (or rely on CI matrix if added)
4. If any FAIL, file an issue and patch before tag
